// PR 6 — placeholder. Admin password setup + sub-key CRUD land in PR 9.

import SwiftUI

struct SecurityScreen: View {
    var body: some View {
        PlaceholderScreen(
            landsIn: .pr9,
            summary: "Admin API key, sub-key CRUD with scopes and rate limits."
        )
    }
}
