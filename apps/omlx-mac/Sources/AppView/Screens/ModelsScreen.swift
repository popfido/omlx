// PR 6 — placeholder. Active models, library, load/unload, plus drill-down
// into per-model Profiles / Basic / Advanced / Aliases land in PR 8.

import SwiftUI

struct ModelsScreen: View {
    var body: some View {
        PlaceholderScreen(
            landsIn: .pr8,
            summary: "Active models, library, load / unload, per-model Profiles · Basic · Advanced · Aliases."
        )
    }
}
