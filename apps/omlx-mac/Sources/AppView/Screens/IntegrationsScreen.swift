// PR 6 — placeholder. Claude Code routing + OpenAI compat defaults land in PR 9.

import SwiftUI

struct IntegrationsScreen: View {
    var body: some View {
        PlaceholderScreen(
            landsIn: .pr9,
            summary: "Claude Code Opus / Sonnet / Haiku routing, context scaling, OpenAI-compat client snippets."
        )
    }
}
